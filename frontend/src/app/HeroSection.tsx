import styles from './page.module.css';

const HeroSection = () => {
    return (
        <section>
            <h1 className={styles.heading}>Super Welcome</h1>
        </section>
    );
};
export default HeroSection;
